os: linux
dist: bionic
language: minimal
cache: pip
python: 3.8
before_script:
  - pip --version
  - pip install --upgrade pip
  - pip install -r requirements.txt
  - sudo npm i -g csvtojson
script: |
      chmod +x generate.sh
      ./generate.sh
